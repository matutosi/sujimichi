% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html.R
\name{html_sujimichi}
\alias{html_sujimichi}
\alias{print.sujimichi_html}
\title{Write the sentences and their links as HTML}
\usage{
html_sujimichi(links, sentences, words = NULL, max_marks = 1, css = TRUE)

\method{print}{sujimichi_html}(x, ...)
}
\arguments{
\item{links}{A data.frame as returned by \code{\link[=connect_sentences]{connect_sentences()}}.}

\item{sentences}{A data.frame as returned by \code{\link[=as_sentences]{as_sentences()}}, with
\code{sentence_id}, \code{paragraph_id} and \code{sentence}.}

\item{words}{A data.frame as returned by \code{\link[=content_words]{content_words()}}, or
\code{NULL} (the default) to mark the shared word on its own.}

\item{max_marks}{A number.  How many shared words to mark in one
line, as in \code{\link[=format_sujimichi]{format_sujimichi()}}.}

\item{css}{A logical.  Put a \verb{<style>} block in front of the lines.}

\item{x}{A \code{sujimichi_html} string.}

\item{...}{Ignored.}
}
\value{
A string of HTML, of class \code{sujimichi_html}.

\code{print()}: \code{x}, invisibly.  The HTML is written as it is, so
that a chunk with \code{results = "asis"} renders it.
}
\description{
The third of the displays sketched in \code{design.md}: the same picture
as \code{\link[=print_sujimichi]{print_sujimichi()}}, marked up with \verb{<span>} so that it can be
pasted into R Markdown or Quarto.
}
\details{
The lines are wrapped in \verb{<pre>}, which keeps the spaces that carry
the alignment.  The widths were counted with \code{\link[stringi:stri_width]{stringi::stri_width()}},
which gives a full-width character two columns, so the alignment
comes out right in a monospaced font that draws them that way.

The shared word of each sentence is put in
\verb{<span class="sujimichi-word">}, and a sentence that shares no word
with any other sentence of its paragraph -- the dead code of
\code{\link[=dead_code]{dead_code()}} -- in \verb{<span class="sujimichi-dead">}.  With
\code{css = TRUE} a \verb{<style>} block for those two classes comes with it,
so that the result stands on its own; pass \code{css = FALSE} and write
the rules yourself when the document already has a style sheet.

In an R Markdown chunk, use \code{results = "asis"}.
}
\examples{
sentences <- as_sentences(sample_text())
words <- data.frame(
  sentence_id = c(1, 1, 2, 2, 3),
  position    = c(1, 5, 1, 7, 1),
  word        = c("文章", "つながり", "つながり", "構造", "構造"))
links <- connect_sentences(words, sentences)
html <- html_sujimichi(links, sentences)
substr(html, 1, 60)

}
