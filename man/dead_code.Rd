% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dead_code.R
\name{dead_code}
\alias{dead_code}
\alias{broken_paragraphs}
\title{Find dead code: sentences that share no word with their paragraph}
\usage{
dead_code(links, sentences)

broken_paragraphs(dead)
}
\arguments{
\item{links}{A data.frame as returned by \code{\link[=connect_sentences]{connect_sentences()}}.
Only \code{sentence_id} and \code{prev_id} are used, so links from either
\code{nearest = TRUE} or \code{FALSE} give the same components.}

\item{sentences}{A data.frame with \code{sentence_id} and
\code{paragraph_id}, as returned by \code{\link[=as_sentences]{as_sentences()}}.  Fixes which
paragraph a sentence belongs to, and lets a sentence outside
\code{links} still get a component of its own.}

\item{dead}{A data.frame as returned by \code{\link[=dead_code]{dead_code()}}.}
}
\value{
A tibble with one row per sentence and columns
\code{sentence_id}, \code{paragraph_id}, \code{component} (numbered from 1
within each paragraph) and \code{isolated} (\code{TRUE} when the sentence is
the only member of its component).

\code{broken_paragraphs()}: the \code{paragraph_id} values that hold
two or more components, sorted.
}
\description{
Treats each paragraph as a graph: a sentence is a node, and a link
from \code{\link[=connect_sentences]{connect_sentences()}} -- a sentence and the earlier sentence it
shares a word with -- is an edge between the two.  The connected
components of this graph are found paragraph by paragraph, as
decided in \code{design.md}.
}
\details{
A sentence alone in its component -- no edge to any earlier or
later sentence of the paragraph -- shares no word with the rest of
the paragraph, and is dead code in the sense of \code{design.md}.
\code{\link[=broken_paragraphs]{broken_paragraphs()}} instead looks at whole paragraphs: two or more
components mean the paragraph splits into parts that share no word
with each other, even when every sentence in it has some connection.
}
\examples{
sentences <- as_sentences(sample_text())
words <- tibble::tribble(
  ~sentence_id, ~position, ~word,
  1, 1, "文章", 1, 5, "単語", 1, 7, "つながり",
  2, 1, "つながり", 2, 9, "文章", 2, 11, "構造",
  3, 1, "構造", 3, 7, "文章")
links <- connect_sentences(words, sentences)
dead_code(links, sentences)

}
