% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content_words.R
\name{analyze_morphemes}
\alias{analyze_morphemes}
\title{Run a morphological analysis on sentences}
\usage{
analyze_morphemes(
  sentences,
  method = "mecab",
  bin_dir = "",
  iconv = "",
  mecabrc = NULL,
  ...
)
}
\arguments{
\item{sentences}{A character vector of sentences, or a data.frame
with a \code{sentence} column as returned by \code{\link[=as_sentences]{as_sentences()}}.}

\item{method}{A string.  Passed to \code{\link[moranajp:moranajp_all]{moranajp::moranajp_all()}}:
"mecab", "ginza", "sudachi_a", "sudachi_b" or "sudachi_c".}

\item{bin_dir}{A string.  Directory of the analyser.}

\item{iconv}{A string.  Encoding conversion of the analyser output,
for example "CP932_UTF-8" when MeCab was built for Shift-JIS.
Leave it empty for a UTF-8 dictionary.}

\item{mecabrc}{A string.  Path of the \code{mecabrc} settings file.
\code{NULL} (the default) looks for it next to \code{bin_dir}.
\code{""} leaves the setting alone.}

\item{...}{Passed to \code{\link[moranajp:moranajp_all]{moranajp::moranajp_all()}}.}
}
\value{
A tibble of morphemes with a \code{sentence_id} column,
or \code{NULL} when the analysis could not be run.
}
\description{
Hands the sentences to \code{\link[moranajp:moranajp_all]{moranajp::moranajp_all()}} and renames the
\code{text_id} column to \code{sentence_id}.  'moranajp' runs 'MeCab',
'Sudachi' or 'Ginza' on the local machine; nothing is sent over the
network.
}
\details{
When 'moranajp' is not installed, or when the analyser cannot be run,
a message is shown and \code{NULL} is returned, so that a script does not
stop on a machine without an analyser.

MeCab on Windows reads its settings from the path it was built with,
and stops when the file is not there.  \code{analyze_morphemes()} therefore
looks for \code{mecabrc} next to \code{bin_dir} and points the \code{MECABRC}
environment variable at it while the analysis runs.  An \code{MECABRC} that
is already set is left alone.
}
\examples{
\dontrun{
  # needs 'moranajp' and a local analyser such as MeCab
  sentences <- as_sentences(sample_text())
  analyze_morphemes(sentences, bin_dir = "d:/pf/mecab/bin")
}

}
