% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content_words.R
\name{sentence_chunks}
\alias{sentence_chunks}
\title{Group sentences into chunks the analyser can swallow}
\usage{
sentence_chunks(sentences, max_bytes = 8000)
}
\arguments{
\item{sentences}{A character vector.}

\item{max_bytes}{An integer.  Largest chunk, counted in bytes.}
}
\value{
An integer vector, the chunk number of each sentence.
}
\description{
Internal function for \code{\link[=analyze_morphemes]{analyze_morphemes()}}.
MeCab reads one line at a time into a buffer of a fixed size, in
bytes, and splits the line when it does not fit; the split falls in
the middle of a character and the text after it is unusable.
'moranajp' joins the sentences into one line and groups them by the
number of characters, which is not the same thing: a Japanese
character takes three bytes in UTF-8, so a group well inside its
character limit can still be far over the buffer.
}
\details{
A sentence longer than \code{max_bytes} on its own is left in a chunk of
its own rather than dropped.
}
\keyword{internal}
