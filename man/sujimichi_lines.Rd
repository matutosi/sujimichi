% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/console_view.R
\name{sujimichi_lines}
\alias{sujimichi_lines}
\alias{format_sujimichi}
\alias{print_sujimichi}
\title{Show sentences on the console with the connecting word aligned}
\usage{
sujimichi_lines(links, sentences, words = NULL, wrap = c("(", ")"))

format_sujimichi(links, sentences, words = NULL, wrap = c("(", ")"))

print_sujimichi(
  links,
  sentences,
  words = NULL,
  wrap = c("(", ")"),
  color = interactive(),
  file = ""
)
}
\arguments{
\item{links}{A data.frame as returned by \code{\link[=connect_sentences]{connect_sentences()}}.}

\item{sentences}{A data.frame with \code{sentence_id} and \code{sentence},
as returned by \code{\link[=as_sentences]{as_sentences()}}.}

\item{words}{A data.frame as returned by \code{\link[=content_words]{content_words()}}, or
\code{NULL} (the default) to mark the shared word on its own.}

\item{wrap}{A character vector of length 2: the marks put around
the shared word.}

\item{color}{A logical.  Colour the marked word with ANSI escapes.
Defaults to \code{\link[=interactive]{interactive()}}, so example runs and \verb{R CMD check}
stay free of escape codes.}

\item{file}{passed to \code{\link[=cat]{cat()}}; \code{""} (the default) prints to the
console.}
}
\value{
A tibble with one row per sentence, in \code{sentence_id} order,
and columns \code{sentence_id}, \code{indent}, \code{before}, \code{marked}, \code{after}.
\code{before}, \code{marked} and \code{after} join back into the full line;
\code{marked} is \code{""} when the sentence has no word to show.

\code{print_sujimichi()}: the plain lines, as from
\code{format_sujimichi()}, invisibly.
}
\description{
Builds one line per sentence, indented so that the word it shares
with its earlier sentence lines up under the same word written in
that earlier line, and marks the word with \code{wrap}.  This is the
console display sketched in \code{design.md}:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{文章とは，単語のつながりである．
               (つながり)があるおかげで，文章の構造を明示できる．
                                              (構造)がわかれば，文章を理解しやすくなる．
}\if{html}{\out{</div>}}

Only the representative link of each sentence (\code{is_main}, from
\code{\link[=connect_sentences]{connect_sentences()}}) is drawn, since one connection per line keeps
the picture readable; a sentence still shows the rest of its text
even when it has no link.  Width is counted with
\code{\link[stringi:stri_width]{stringi::stri_width()}}, so full-width characters take two columns.

The indent of a line is the width of the text before the word in
the \emph{earlier} line, minus one column, added to that earlier line's
own indent -- so the mark overlaps the last column of the text it
points at, as in the example above.

A word (a lemma) is looked for as a literal substring of the
sentence text.  When it is not found -- for example a verb whose
lemma differs from the form actually written -- the sentence is
shown as plain text, at the same indent as the sentence it links to.

An analyser splits a compound word that is not in its dictionary:
MeCab with ipadic reads 「畦畔」 as 「畦」 and 「畔」, and marking
「(畦)畔」 reads oddly even though the link itself is sound.  Give
\code{words} and the mark is widened over the nouns next to the shared
one, so that the whole compound is marked.  Only the mark changes;
the links were worked out in \code{\link[=connect_sentences]{connect_sentences()}} and are untouched.
}
\examples{
sentences <- as_sentences(sample_text())
words <- tibble::tribble(
  ~sentence_id, ~position, ~word,
  1, 1, "文章", 1, 5, "単語", 1, 7, "つながり",
  2, 1, "つながり", 2, 9, "文章", 2, 11, "構造",
  3, 1, "構造", 3, 7, "文章")
links <- connect_sentences(words, sentences)
sujimichi_lines(links, sentences)

}
