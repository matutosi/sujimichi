% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect_sentences.R
\name{check_words}
\alias{check_words}
\alias{sentence_ids}
\alias{link_rows}
\alias{keep_first_links}
\alias{add_lonely_rows}
\alias{link_weight}
\alias{count_referred}
\title{Steps of connect_sentences()}
\usage{
check_words(words)

sentence_ids(words, sentences = NULL)

link_rows(words, para, nearest = TRUE)

keep_first_links(linked, max_links = Inf)

add_lonely_rows(linked, all_ids)

link_weight(distance, how = "inverse")

count_referred(linked)
}
\arguments{
\item{words}{A data.frame of content words with columns
\code{sentence_id}, \code{position} and \code{word}, as returned by
\code{\link[=pick_content_words]{pick_content_words()}} or \code{\link[=content_words]{content_words()}}.}

\item{sentences}{A data.frame of sentences with columns
\code{sentence_id} and \code{paragraph_id}, as returned by \code{\link[=as_sentences]{as_sentences()}}.
It fixes the paragraph a sentence belongs to, and lets a sentence
without any content word appear in the table.}

\item{para}{A named vector of paragraph numbers, named by sentence
number.}

\item{nearest}{A logical.  \code{TRUE} (the default) keeps only the
nearest earlier sentence for each word.  \code{FALSE} keeps every
earlier sentence that holds the word.}

\item{linked}{A data.frame of links.}

\item{max_links}{A number.  The largest number of links kept for one
sentence, counted from the front of the sentence.  \code{Inf} (the
default) keeps them all; \code{3} follows the option in \code{design.md}.
Over a review paper of 461 sentences a sentence had five links in
the middle of the range, and cutting at three threw away half of
them, which is why the default keeps them all and the cut is left
to the display.}

\item{all_ids}{A vector of every sentence number.}

\item{distance}{A numeric vector.}

\item{how}{A string: \code{"inverse"} or \code{"distance"}.}
}
\value{
A data.frame, or a vector for \code{count_referred()}.
}
\description{
Internal functions for \code{\link[=connect_sentences]{connect_sentences()}}.
}
\details{
\itemize{
\item \code{check_words()} makes sure the table of content words can be used,
and sorts it by sentence and by place in the sentence.
\item \code{sentence_ids()} settles which sentences to walk through and which
paragraph each belongs to.
\item \code{link_rows()} builds one row per link.
\item \code{keep_first_links()} keeps at most \code{max_links} rows per sentence,
counted from the front of the sentence, and marks the first of them
as the representative.
\item \code{add_lonely_rows()} adds an empty row for a sentence without a link.
\item \code{link_weight()} turns the distance into a weight.
\item \code{count_referred()} counts, for each sentence, the later sentences
that look back at it.
}
}
\keyword{internal}
