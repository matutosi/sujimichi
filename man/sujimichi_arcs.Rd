% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{sujimichi_arcs}
\alias{sujimichi_arcs}
\title{Points of the arcs that join the sentences}
\usage{
sujimichi_arcs(
  links,
  sentences = NULL,
  max_links = 3,
  paragraph = NULL,
  bulge = 0.5,
  n = 32
)
}
\arguments{
\item{links}{A data.frame as returned by \code{\link[=connect_sentences]{connect_sentences()}}.}

\item{sentences}{A data.frame as returned by \code{\link[=as_sentences]{as_sentences()}}, used
to pick paragraphs.  May be \code{NULL} when \code{paragraph} is \code{NULL}.}

\item{max_links}{A number.  How many links to draw for one sentence,
counted from the front of the sentence.  \code{3} follows the option in
\code{design.md}.}

\item{paragraph}{A vector of \code{paragraph_id} to keep, or \code{NULL} (the
default) for all of them.  A whole book at once is unreadable, so
a few paragraphs at a time is the usual way to look at this.}

\item{bulge}{A number.  How far an arc bulges out, as a share of the
distance it spans.}

\item{n}{An integer.  Points drawn along one arc.}
}
\value{
A tibble with one row per point and columns \code{link_id},
\code{sentence_id}, \code{prev_id}, \code{word}, \code{distance}, \code{weight}, \code{x}, \code{y}.
\code{link_id} groups the points of one arc.
}
\description{
Lays the sentences out on a vertical axis, one row each, and draws
the link of each sentence as an arc back to the sentence it shares a
word with.  This is the display sketched in \code{design.md} as the
'ggplot2' one; it is returned as data, so that the geometry can be
looked at, drawn with something else, or tested without 'ggplot2'.
}
\details{
An arc is half an ellipse: it runs from \code{prev_id} up to
\code{sentence_id} on the y axis, and bulges out to
\code{bulge * (distance / 2)} on the x axis.  A link that reaches further
back therefore bulges further out.
}
\examples{
sentences <- data.frame(paragraph_id = c(1, 1, 1),
                        sentence_id  = c(1, 2, 3))
words <- data.frame(
  sentence_id = c(1, 1, 2, 2, 3),
  position    = c(1, 5, 1, 7, 1),
  word        = c("文章", "つながる", "つながる", "構造", "構造"))
links <- connect_sentences(words, sentences)
sujimichi_arcs(links, sentences)

}
