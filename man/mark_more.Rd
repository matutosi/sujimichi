% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/console_view.R
\name{mark_more}
\alias{mark_more}
\title{Mark the other shared words of a line}
\usage{
mark_more(lines, links, words = NULL, wrap = c("(", ")"), max_marks = 1)
}
\arguments{
\item{lines}{A data.frame as returned by \code{\link[=sujimichi_lines]{sujimichi_lines()}}.}

\item{links}{A data.frame as returned by \code{\link[=connect_sentences]{connect_sentences()}}.}

\item{words}{A data.frame as returned by \code{\link[=content_words]{content_words()}}, or \code{NULL}.}

\item{wrap}{A character vector of length 2.}

\item{max_marks}{A number.}
}
\value{
A character vector, the \code{after} part of each line.
}
\description{
Internal function for \code{\link[=format_sujimichi]{format_sujimichi()}}.
\code{design.md} asks for an option to show up to three connections.  The
representative settles the indent and is marked by
\code{\link[=sujimichi_lines]{sujimichi_lines()}}; the others are marked here, in the text that
follows it, so that the alignment worked out from the text before the
representative is left alone.  A word that is not written as its
lemma says is passed over.
}
\details{
The other words are widened over a split compound in the same way as
the representative, and a word that the widening has already brought
into an earlier mark is passed over, so that 「畦」 and 「畔」 give
one 「(畦畔)」 rather than 「(畦)(畔)」.
}
\keyword{internal}
