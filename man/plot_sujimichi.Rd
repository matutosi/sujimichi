% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_sujimichi}
\alias{plot_sujimichi}
\title{Draw the sentences and their links with 'ggplot2'}
\usage{
plot_sujimichi(
  links,
  sentences,
  max_links = 3,
  paragraph = NULL,
  bulge = 0.5,
  label = TRUE,
  chars = 30,
  ratio = 1
)
}
\arguments{
\item{links}{A data.frame as returned by \code{\link[=connect_sentences]{connect_sentences()}}.}

\item{sentences}{A data.frame as returned by \code{\link[=as_sentences]{as_sentences()}}, with
\code{sentence_id}, \code{paragraph_id} and \code{sentence}.}

\item{max_links}{A number.  How many links to draw for one sentence,
counted from the front of the sentence.  \code{3} follows the option in
\code{design.md}.}

\item{paragraph}{A vector of \code{paragraph_id} to keep, or \code{NULL} (the
default) for all of them.  A whole book at once is unreadable, so
a few paragraphs at a time is the usual way to look at this.}

\item{bulge}{A number.  How far an arc bulges out, as a share of the
distance it spans.}

\item{label}{A logical.  Write the text of each sentence beside its
row.}

\item{chars}{An integer.  Longest label, in characters; what is left
over is cut and marked with an ellipsis.}

\item{ratio}{A number, passed to \code{\link[ggplot2:coord_fixed]{ggplot2::coord_fixed()}}.  One unit
across the panel is \code{ratio} sentences down it, so an arc keeps the
shape of the distance it spans; left free, the arcs stretch to fill
the panel and a near link cannot be told from a far one.  Raise it
to squeeze the arcs, lower it to spread them out.}
}
\value{
A 'ggplot' object, or \code{NULL} when 'ggplot2' is missing.
}
\description{
Puts one sentence on each row and joins it to the sentence it shares
a word with by an arc, as sketched in \code{design.md}.  A sentence that
shares no word with any other sentence of its paragraph -- the dead
code of \code{\link[=dead_code]{dead_code()}} -- is drawn in a second colour, so that it can
be picked out at a glance.
}
\details{
'ggplot2' is in \code{Suggests}: when it is not installed a message is
shown and \code{NULL} is returned, and the rest of the package still
works.  \code{\link[=sujimichi_arcs]{sujimichi_arcs()}} returns the same geometry as data for a
reader who would rather draw it another way.
}
\examples{
\dontrun{
  # needs 'ggplot2'
  sentences <- as_sentences(sample_text())
  words <- data.frame(
    sentence_id = c(1, 1, 2, 2, 3),
    position    = c(1, 5, 1, 7, 1),
    word        = c("文章", "つながり", "つながり", "構造", "構造"))
  links <- connect_sentences(words, sentences)
  plot_sujimichi(links, sentences)
}

}
