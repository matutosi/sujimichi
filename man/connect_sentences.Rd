% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connect_sentences.R
\name{connect_sentences}
\alias{connect_sentences}
\title{Connect each sentence to the earlier sentences it shares a word with}
\usage{
connect_sentences(
  words,
  sentences = NULL,
  nearest = TRUE,
  max_links = Inf,
  weight = c("inverse", "distance")
)
}
\arguments{
\item{words}{A data.frame of content words with columns
\code{sentence_id}, \code{position} and \code{word}, as returned by
\code{\link[=pick_content_words]{pick_content_words()}} or \code{\link[=content_words]{content_words()}}.}

\item{sentences}{A data.frame of sentences with columns
\code{sentence_id} and \code{paragraph_id}, as returned by \code{\link[=as_sentences]{as_sentences()}}.
It fixes the paragraph a sentence belongs to, and lets a sentence
without any content word appear in the table.}

\item{nearest}{A logical.  \code{TRUE} (the default) keeps only the
nearest earlier sentence for each word.  \code{FALSE} keeps every
earlier sentence that holds the word.}

\item{max_links}{A number.  The largest number of links kept for one
sentence, counted from the front of the sentence.  \code{Inf} (the
default) keeps them all; \code{3} follows the option in \code{design.md}.
Over a review paper of 461 sentences a sentence had five links in
the middle of the range, and cutting at three threw away half of
them, which is why the default keeps them all and the cut is left
to the display.}

\item{weight}{One of \code{"inverse"} (the default) or \code{"distance"},
the two ways \code{design.md} leaves open for turning the distance into
a weight.  \code{"inverse"} gives \code{1 / distance}, so that a nearer
sentence weighs more and the number reads the same way round as
the other measures; \code{"distance"} gives the distance itself.}
}
\value{
A tibble with one row per link and these columns.
\itemize{
\item \code{sentence_id}: the sentence that looks back.
\item \code{word}: the shared word, as a lemma.  \code{NA} when the sentence
shares no word.
\item \code{position}: the place of the word in its sentence, counted over
all morphemes.  The smaller, the closer to the front.
\item \code{prev_id}: the earlier sentence that holds the same word.
\item \code{distance}: \code{sentence_id - prev_id}.  The smaller, the closer.
\item \code{weight}: the distance turned into a weight, see \code{weight}.
Over a review paper 69 per cent of the links ran to the sentence
right before, so the weight is close to 1 most of the time and a
link reaching further back stands out.
\item \code{is_main}: whether the row is the representative of its sentence.
\item \code{referred}: how many later sentences look back at \code{sentence_id}.
A high count marks a sentence that the rest of the paragraph
leans on, such as a topic sentence.
}
}
\description{
Walks through the content words of a text and looks back for an
earlier sentence that holds the same word.  One row is returned per
link, that is per pair of a word and the earlier sentence it points
to.  A sentence that shares no word with any earlier sentence of its
paragraph gets one row with \code{NA} in \code{word} and \code{prev_id}, so that
every sentence appears in the table.
}
\details{
Only the earlier sentences of the same paragraph are searched, as
decided in \code{design.md}.  When \code{sentences} is not given, the whole text
is treated as one paragraph.

The word that comes first in a sentence is taken as the
representative of that sentence, and gets \code{is_main = TRUE}.
A tie is settled by the nearer earlier sentence.
}
\examples{
sentences <- data.frame(
  paragraph_id = c(1, 1, 1),
  sentence_id  = c(1, 2, 3))
words <- data.frame(
  sentence_id = c(1, 1, 2, 2, 3),
  position    = c(1, 5, 1, 7, 1),
  word        = c("文章", "つながる", "つながる", "構造", "構造"))
connect_sentences(words, sentences)

}
