% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markdown.R
\name{strip_markdown}
\alias{strip_markdown}
\title{Strip Markdown notation to plain text}
\usage{
strip_markdown(text)
}
\arguments{
\item{text}{A character vector, one element per line.}
}
\value{
A character vector of lines, with Markdown notation removed.
}
\description{
Removes Markdown syntax markers while keeping the body text, so the
result can be handed to \code{\link[=as_sentences]{as_sentences()}}.  A fenced code block
(\verb{```} or \verb{~~~}) is removed entirely, fence lines and content
alike, because code is not prose and should not be split into
sentences.  A heading, a list marker and a blockquote marker are
each removed, but the text after them is kept.  An inline code span
keeps its content and loses only the backticks.
}
\examples{
strip_markdown(c("# 見出し", "",
                  "本文だ．", "",
                  "- 箇条書き1", "- 箇条書き2"))

}
